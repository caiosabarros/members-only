<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
       <%#<td><strong><%=post.user.name</strong>></td>%>
      <%#<td><strong><%=post.user.name</strong>></td>%>
        <td><%= post.title %></td>
        <td><%= post.body %></td>
        
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>
